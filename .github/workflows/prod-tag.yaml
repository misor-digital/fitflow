name: Production Tag generation

on:
  push:
    branches: [main]

jobs:
  generate-prod-tag:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # safer for tags

      - uses: pnpm/action-setup@v4
        with:
          version: 10.25.0

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Version with changesets (no-op if none)
        id: version
        shell: bash
        run: |
          set -e

          pnpm changeset version || true

          if git diff --quiet; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
            echo "No version changes to commit/tag."
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Commit version bump
        if: steps.version.outputs.changed == 'true'
        shell: bash
        run: |
          set -e
          VERSION=$(node -p "require('./package.json').version")
          TAG="v$VERSION"

          git config user.name "misor-digital"
          git config user.email "misor.digital@gmail.com"
          git add -A
          git commit -m "Update version: bump to $VERSION"
          git push

      - name: Tag release (only if new commit happened)
        if: steps.version.outputs.changed == 'true'
        shell: bash
        run: |
          set -e
          VERSION=$(node -p "require('./package.json').version")
          TAG="v$VERSION"
          
          if git rev-parse "$TAG" >/dev/null 2>&1; then
            echo "Tag $TAG already exists, skipping."
            exit 0
          fi

          git tag "$TAG"
          git push origin "$TAG"
